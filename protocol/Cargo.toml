[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nalgebra = "0.30.1"
async-compression = { version = "0.3.15", features = ["tokio", "zlib"] }
rgb = "0.8.33"
tokio = { version = "1.23.0", features = ["full"] }
strum_macros = "0.24.3"
strum = "0.24.1"